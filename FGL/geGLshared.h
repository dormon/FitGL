#pragma once
#include "Shared.h"
#include <geGL/geGL.h>
namespace ge{
  namespace gl{
    classShared(AsynchronousQuery);
    classShared(Buffer);
    classShared(Framebuffer);
    classShared(Shader);
    classShared(Program);
    classShared(VertexArray);
    classShared(ProgramPipeline);
    classShared(Texture);
    classShared(Sampler);
    classShared(Renderbuffer);
    classShared(DebugMessage);
    classShared(FunctionLoaderInterface);
    classShared(DefaultLoader);
  }
};
